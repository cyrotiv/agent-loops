# Generator Prompt — Phase 3 Build Loop

<!-- STUB — filled in before Phase 3.
     Will enforce, every iteration:
     1. Read feature_list.json, progress.md, contract.md first.
     2. Pick the single next unfinished feature.
     3. Boot/reset the simulator to a clean state.
     4. Implement it; run build + unit tests + relevant Maestro flow; fix breakage.
     5. Update feature_list.json + progress.md; append to log.md; commit.
     6. If all features pass the contract, write DONE as last line of progress.md.
     Plus the working agreements from PLAN.md Phase 3, verbatim:
     think before coding (unattended variant), simplicity first,
     surgical changes, goal-driven execution. -->
