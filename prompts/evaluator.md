# Evaluator Prompt — Phase 4 Evaluation Loop

<!-- STUB — filled in before Phase 2/4.
     Runs on GLM-5.2 (code eval) via: kimi -m "zai/glm-5.2" -p "$(cat prompts/evaluator.md)"
     System stance: "the code is broken, your job is to prove it."
     Will encode: read contract.md; run build, unit tests, every Maestro flow
     on a clean simulator; pass/fail per assertion citing a specific contract
     line or test run (never an impression); append verdicts to log.md;
     write failures to progress.md as blockers; report suspected generator
     over-engineering as a finding, never fix it — roles stay separated. -->
